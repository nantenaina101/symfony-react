{% extends 'base.html.twig' %}

{% block title %}Nos produits
{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-md-3">
			<h1>Filtrer</h1>
			{{ form(form) }}

			</div>
			<div class="col-md-9">
				<h1>Nos produits</h1>
				<div class="row">
					{% for product in products %}

						<div class="col-md-4 mb-3 text-center">
							<div class="product-item">
								<a href="{{ path('product', {'slug' : product.slug}) }}"><img src="/uploads/{{product.illustration}}" alt="{{ product.name }}" class="img-fluid"></a>
								<a href="{{ path('product', {'slug' : product.slug}) }}">
									<h5>{{ product.name }}</h5>
								</a>
								<span>{{ product.subtitle }}</span><br>
								<span class="font-weight-bold">{{ (product.price / 100) | number_format(2, ',', '.') }}
									€</span>

							</div>
						</div>

					{% endfor %}
				</div>

			</div>
		</div>
		<hr class="featurette-divider">

	{% endblock %}
